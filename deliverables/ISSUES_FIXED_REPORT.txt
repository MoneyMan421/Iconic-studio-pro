Iconic Studio Pro - Issue Fix Report
Date: 2026-05-14

Fixed in this pass:
1) Paywall upgrade flow now unlocks Pro locally and persists unlock state.
   - Files: lib/main.dart, lib/editor_storage.dart
   - Result: Upgrade button no longer acts like a no-op; free import gate can be lifted.

2) Import gate now respects persisted Pro state.
   - Files: lib/main.dart, lib/editor_storage.dart
   - Result: Unlimited imports are enabled after local Pro unlock.

3) Auth login hardening.
   - File: lib/auth_screen.dart
   - Result: Login now requires a stored password hash and always verifies password.

4) Misleading paywall feature copy removed.
   - File: lib/main.dart
   - Result: "Cloud sync" no longer advertised as a shipped feature.

5) Fake hardcoded FPS removed.
   - File: lib/main.dart
   - Result: FPS stat now uses live frame timing data.

6) Widget test source-file check made path-safe.
   - File: test/widget_test.dart
   - Result: Test resolves lib/main.dart via package URI instead of brittle relative path.

7) Removed unused flutter_svg import from main editor screen.
   - File: lib/main.dart

8) Replaced deprecated web export API.
   - Files: lib/export_web.dart, pubspec.yaml
   - Result: Web export now uses package:web + dart:js_interop instead of deprecated dart:html.

9) Removed unused empty asset bundle declaration.
   - Files: pubspec.yaml, .github/workflows/ci.yml, .github/copilot-instructions.md
   - Result: CI and app config no longer require the empty assets/icons/ directory.

10) Added repository CodeQL workflow.
    - File: .github/workflows/codeql.yml
    - Result: GitHub code scanning now has an in-repo workflow that analyzes the Android/Kotlin surface instead of relying on missing default setup.

Validation status in this environment:
- Flutter CLI is not available here, so analyze/test/build could not be run.
- Command attempted: flutter --version (failed: command not found)

User must:
- No additional manual code changes are required from the user for the issues addressed in this pass.
- Review CI / CodeQL results after GitHub Actions runs on the pushed branch.
