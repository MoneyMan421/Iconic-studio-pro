import 'dart:typed_data';

/// Fallback stub — should never be reached on a supported Flutter target.
Future<String> saveExportedIcon(Uint8List bytes) async {
  throw UnimplementedError('Export is not supported on this platform.');
}
